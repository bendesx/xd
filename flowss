<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flor de Cumpleaños</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f8ff;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .flower {
            position: relative;
            width: 200px;
            height: 200px;
            transform: rotate(45deg);
        }

        .petal {
            position: absolute;
            width: 100px;
            height: 100px;
            background-color: #ff69b4;
            border-radius: 50% 50% 0 0;
            transform-origin: bottom right;
            animation: bloom 2s ease-in-out forwards;
        }

        .petal:nth-child(1) { transform: rotate(0deg); }
        .petal:nth-child(2) { transform: rotate(90deg); }
        .petal:nth-child(3) { transform: rotate(180deg); }
        .petal:nth-child(4) { transform: rotate(270deg); }

        .center {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 80px;
            height: 80px;
            background-color: yellow;
            border-radius: 50%;
            transform: translate(-50%, -50%);
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 18px;
            color: #ff69b4;
            opacity: 0;
            animation: reveal 2s 2s forwards;
        }

        @keyframes bloom {
            from { transform: scaleY(0); }
            to { transform: scaleY(1); }
        }

        @keyframes reveal {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="flower">
        <div class="petal"></div>
        <div class="petal"></div>
        <div class="petal"></div>
        <div class="petal"></div>
        <div class="center">Feliz Cumpleaños</div>
    </div>
</body>
</html>
